name: Pipeline for test

on:
    push:
        branches:
        - "main"
        - "develop"


jobs:
    tests:
        runs-on: ubuntu-latest
    
    steps:
        uses: actions/checkout@v3

            actions/setup-node@v3
        with:
            node-version: latest

        run: npm install

        run: npm test
        

        
